/**
 * Cost Sharing Percentage (CSP) flag.
 *
 * Indicates whether the project collects a Cost Sharing Percentage.
 * When `NO`, the user must supply a non-collection reason.
 */
export enum CspFlag {
  YES = 'YES',
  NO = 'NO',
}
